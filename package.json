{
  "name": "backertracker-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "web-ext lint --warnings-as-errors",
    "build": "web-ext build",
    "dev": "web-ext run",
    "dev:chrome": "web-ext run --target chromium"
  },
  "devDependencies": {
    "jsdom": "^27.0.1",
    "vitest": "^3.2.7",
    "web-ext": "^10.7.0"
  }
}
